# panicshutdown
a minimal utility for an almost instant safe-ish shutdown  

does the equivalent of 's', 'u', and 'o' in /proc/sysrq-trigger but via syscalls  
must be run as root, feel free to use setuid
